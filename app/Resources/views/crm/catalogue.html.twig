{% extends 'layout.html.twig' %}

{% block content %}
    <h1 class="container-fluid">
        Liste des produits
    </h1>
    <div class="container-fluid action-buttons">
        <a href="{{ path('edit_product',  { id: 0 }) }}" class="btn btn-primary">Ajouter </a>
    </div>
    <div class="container-fluid list-container">
        <table id="document-list">
            <thead>
            <tr>
                <th>Reference</th>
                <th>Désignation</th>
                <th>Prix U HT</th>
                <th>Qté</th>
                <th>Fournisseur</th>

            </tr>
            </thead>
            <tbody>
            {% for produit in cataloguetwig %}
                <tr>
                    <td><a href="#">{{ produit.reference }}</a> </td>
                    <td>{{ produit.designation }}</td>
                    <td>{{ produit.prixUnitaire }}</td>
                    <td>{{ produit.quantite }}</td>
                    <td>{{ produit.fournisseurs }}</td>

                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

